import 'package:flutter/material.dart';

class ServiseTile extends StatelessWidget {
  const ServiseTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
      child: const Text('ServiseTile'),
    );
  }
}
