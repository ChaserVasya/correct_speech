import 'package:flutter/material.dart';

class StudentsEditingScreen extends StatelessWidget {
  const StudentsEditingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
